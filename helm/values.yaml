replicaCount: 1

image:
  repository: link-shortener-notification-service
  pullPolicy: IfNotPresent
  tag: "latest"

serviceAccount:
  name: link-shortener-notification-service

spring:
  profiles:
    active: "default"

resources:
  requests:
    memory: "256Mi"
    cpu: "500m"
  limits:
    memory: "512Mi"
    cpu: "1"

env:
  - name: JAVA_TOOL_OPTIONS
    value: "-XX:MaxRAMPercentage=75.0"

vault:
  role: ""
  secretPath: ""
  secretTemplate: ""